### Setting

install PHP-7.4+

install MySQL

install Composer

install cd /var/www/

git clone ... folder

cd folder

composer install

define database, APP_KEY in .env

php artisan migrate --seed

php artisan serve
